# Environment
.env
.env.local
.env.*.local

# Vercel
.vercel

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (falls später mal Tooling dazukommt)
node_modules/

# Bildschirmfotos des Responsive-Prüfstands (responsivecheck.js).
# Werden bei jedem Lauf neu erzeugt — über hundert PNGs, die niemand
# versionieren will. Der Prüfstand selbst gehört ins Repo.
shots/

# GIMP-Quellen — zu groß für GitHub, gehören nicht ins Deployment
*.xcf

# Lehrwerks-Vokabellisten (Verlags-PDFs und alles, was daraus
# entsteht). Eine Wortliste ist als Sammlung geschützt; dieses Repo
# ist öffentlich. Die Regel steht in
# MPSkills/tools/wordisland/tools/greenline.mjs, der Inhalt bleibt
# lokal und geht von dort direkt in die Datenbank.
MPSkills/tools/wordisland/Englisch Greenline Vokabellisten/
